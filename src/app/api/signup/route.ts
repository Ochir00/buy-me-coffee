const userdata = [];
import { NextResponse } from "next/server";
export function POST(): Promise<NextResponse> {
    const { username, password, email, profileId, bankCard } = res.json
    const burtgelshalgah = 
    try {
        
    }
    catch (error) {

    }
}